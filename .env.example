# 复制为 .env 并按需填写。所有 VITE_* 变量只在构建时静态注入，勿放置密钥。
# 各环境配置见：.env.development / .env.test / .env.production

# API 基础地址。
# - 开发模式：默认使用 Ant Design 官方 Demo API；
#   如需本地 mock（mock/ 目录 + config/proxy.mts 代理），留空即可。
# - 测试环境：留空走 config/proxy.mts 代理；部署静态站点时填完整地址。
# - 生产环境：接入真实后端时改为你的服务地址。
VITE_API_BASE_URL=
