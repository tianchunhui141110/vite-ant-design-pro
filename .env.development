# 开发环境（npm run dev / vite）
#
# 继续使用 Ant Design 官方 Demo API。
# 如需改用本地 mock（mock/ 目录 + config/proxy.mts 代理），将此值留空即可。
VITE_API_BASE_URL=https://pro-api.ant-design-demo.workers.dev

# 权限来源模式：frontend（本地配置按角色过滤）| backend（后端接口返回菜单）
VITE_ACCESS_MODE=frontend
