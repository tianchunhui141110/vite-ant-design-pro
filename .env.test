# 测试环境（npm run start:test / vite --mode test）
#
# 默认走 config/proxy.mts 中 test 的代理（/api -> Ant Design 官方 Demo API），
# 本地开发时留空即可（请求走 dev server 代理）。
# 若部署为无代理的静态站点，请改为完整地址，例如：
# VITE_API_BASE_URL=https://pro-api.ant-design-demo.workers.dev
VITE_API_BASE_URL=
